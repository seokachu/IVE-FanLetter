import LetterForm from "../fanletter/LetterForm";
import LetterList from "../fanletter/LetterList";

const Main = () => {
  return (
    <main>
      <LetterForm />
      <LetterList />
    </main>
  );
};

export default Main;
