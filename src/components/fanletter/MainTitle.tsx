import TextTyping from "@/utils/TextTyping";
import S from "@/styles/style.module.scss";

const MainTitle = () => {
  return (
    <section className={S.mainTitle}>
      <TextTyping />
    </section>
  );
};

export default MainTitle;
